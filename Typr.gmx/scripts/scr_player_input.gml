//List of keys the player can press
esc = keyboard_check_pressed(vk_escape);

//Input from keyboard for in-game modes
player_input = keyboard_string;

//Any key pressed
any_key = keyboard_check_pressed(vk_anykey);
bk_space = keyboard_check_pressed(vk_backspace);

